# Claude Certified Architect – Practice Quiz & Explanations

## Topic 1: Agentic Architecture & Orchestration

### Question 1
An automated customer support resolution agent processes multi-turn account recovery workflows. Which mechanism must be used to decide whether the agentic loop continues tool execution or completes the turn?

* **A)** Parse natural language phrases such as "I have finished processing" in the assistant's response.
* **B)** Inspect the API response `stop_reason`: execute requested tools when `stop_reason` is `"tool_use"`, and terminate the loop when `stop_reason` is `"end_turn"`.
* **C)** Enforce a strict iteration cap of 5 turns as the primary stopping condition for all requests.
* **D)** Check whether the cumulative tool execution output block exceeds 1,000 tokens.

---

### Question 2
A multi-agent research system uses a coordinator agent that invokes a document analysis subagent using the `Task` tool. Why did the subagent fail to incorporate background guidelines provided in the coordinator's system prompt?

* **A)** The `Task` tool automatically strips system prompt directives when initializing subagent loops.
* **B)** The coordinator agent failed to set `tool_choice: "any"` when spawning the subagent execution context.
* **C)** Subagents operate with isolated context and do not automatically inherit parent conversation history; required context must be explicitly passed in the subagent prompt.
* **D)** Subagents only share memory with the coordinator if both are defined in the same user-level configuration.

---

### Question 3
In a multi-agent research pipeline, a coordinator agent needs to analyze three distinct source documents simultaneously to reduce research latency. How should the coordinator spawn these subagents?

* **A)** Emit multiple `Task` tool calls within a single response turn to execute subagents in parallel.
* **B)** Emit one `Task` tool call per turn sequentially, waiting for `stop_reason: "end_turn"` after each subagent completes.
* **C)** Execute `fork_session` in the main session before invoking each `Task` tool call.
* **D)** Pass an array of document paths inside a single parameter to a single subagent instance.

---

### Question 4
In a customer resolution agent, strict compliance policies dictate that customer identity must be verified via `get_customer` before any monetary refund via `process_refund` can occur. What design guarantees compliance?

* **A)** Add 5 few-shot examples in the system prompt demonstrating `get_customer` being invoked before `process_refund`.
* **B)** Include explicit prompt instructions stating that identity verification is mandatory prior to refund execution.
* **C)** Set `tool_choice: {"type": "tool", "name": "get_customer"}` on every incoming prompt.
* **D)** Implement programmatic prerequisite checks/hooks that intercept `process_refund` calls and block execution until `get_customer` has returned a verified customer ID.

---

### Question 5
An enterprise developer tool built on the Claude Agent SDK must prevent autonomous file deletion or execution of dangerous shell commands above a security risk threshold. Which SDK feature enforces this policy deterministically?

* **A)** High-level system prompt instructions prohibiting destructive commands.
* **B)** Tool call interception hooks that evaluate outgoing parameters and block policy-violating actions before execution.
* **C)** Configuring `context: fork` across all built-in file editing tool definitions.
* **D)** Validating tool output strings during the `Post ToolUse` hook phase.

---

### Question 6
A research coordinator assigned to analyze "The Impact of Artificial Intelligence on Creative Industries" spawned subagents only for digital art and graphic design, omitting music and literature. How should the coordinator prompt be designed to rectify this narrow task decomposition?

* **A)** Force the coordinator to execute a static, fixed sequential pipeline across pre-defined domains.
* **B)** Instruct the coordinator to analyze query requirements, evaluate synthesis output for coverage gaps, and iteratively re-delegate targeted sub-queries to subagents until coverage criteria are met.
* **C)** Disable subagent spawning and force the coordinator to run all web searches directly.
* **D)** Restrict subagent tool access to prevent filtering of search results.

---

### Question 7
An engineer exploring a complex refactoring task wants to branch off from a shared codebase analysis baseline to compare two distinct refactoring architectures without losing the original baseline. Which session command or mechanism should be used?

* **A)** Run `--resume` with the same session identifier twice sequentially.
* **B)** Use `fork_session` to create independent execution branches from the shared analysis baseline.
* **C)** Initialize a new session and re-execute all discovery `Grep` queries.
* **D)** Execute `/compact` to clear the tool execution context.

---

### Question 8
Tools returning customer support data output dates in heterogeneous formats (Unix timestamps, ISO 8601, and localized string dates), causing model errors during chronological analysis. What architectural pattern resolves this issue?

* **A)** Implement a `Post ToolUse` hook to intercept and normalize heterogeneous tool outputs into a standard format before appending them to the conversation context.
* **B)** Instruct the agent in the system prompt to convert all timestamps using internal chain-of-thought logic.
* **C)** Force tools to raise validation exceptions whenever non-ISO dates are returned.
* **D)** Route all timestamp comparisons through human-in-the-loop verification steps.

---

### Question 9
When tasked with adding unit test coverage to a legacy, unformatted code module with complex cross-file dependencies, how should the agent approach task decomposition?

* **A)** Modify all module files simultaneously in a single execution turn.
* **B)** Apply dynamic adaptive decomposition: map module structure first, identify entry points, create a prioritized plan, and execute changes incrementally while running tests after each modification.
* **C)** Execute a single prompt chaining script that edits code and generates tests in one pass.
* **D)** Set `tool_choice: "any"` to force file editing tools before analyzing dependencies.

---

### Question 10
A coordinator agent needs to pass research findings from multiple document analysis subagents to a report synthesis subagent while preserving claim-source mappings. How should the data be structured?

* **A)** Concatenate all raw prose outputs into an unstructured string block.
* **B)** Summarize findings into bullet points and strip out document names to save context tokens.
* **C)** Format findings using structured schemas (e.g., JSON/YAML) that separate core claim text from metadata headers like source URLs, document titles, and publication dates.
* **D)** Require the synthesis subagent to re-read all original files independently.

---

## Topic 2: Tool Design & MCP Integration

### Question 1
Production logs show that a customer support agent frequently calls `get_customer` when users ask about order status because `get_customer` and `lookup_order` have minimal, overlapping descriptions. What is the most effective first step to improve tool selection reliability?

* **A)** Combine both tools into a single database search utility.
* **B)** Expand each tool's description to clearly define its specific purpose, expected input formats, example queries, boundary conditions, and when to use it versus similar alternatives.
* **C)** Enforce `tool_choice: "any"` across all turns in the agent loop.
* **D)** Add a regex pre-filter in application code to block `get_customer` calls when digits are present in the query.

---

### Question 2
An MCP tool integration encounters a transient database connection timeout while fetching user profiles. How should the MCP tool signal this error to enable intelligent agent recovery?

* **A)** Return a generic string `"Operation failed"` with `isError: true`.
* **B)** Raise an unhandled HTTP 500 error to terminate the agent loop immediately.
* **C)** Return structured error metadata with `isError: true`, including `errorCategory: "transient"`, `isRetryable: true`, and a clear description.
* **D)** Return an empty success JSON payload with `isError: false` to hide the failure.

---

### Question 3
A synthesis subagent in a research pipeline frequently attempts web searches, failing due to missing credentials and hallucinating facts. How should tool distribution be configured across agents?

* **A)** Restrict each subagent's tool set strictly to those required for its specific role (scoped access), giving search tools only to dedicated search subagents.
* **B)** Grant all subagents unrestricted access to all registered MCP tools.
* **C)** Configure `tool_choice: "auto"` on the synthesis agent while keeping all search tools attached.
* **D)** Move search tools from project configuration to user-level configuration (`~/.claude.json`).

---

### Question 4
An engineering team wants to configure a shared MCP server for internal Jira issue tracking so that all developers automatically receive the integration when cloning the repository. How should this server be configured?

* **A)** In `~/.claude.json` in each developer's home directory.
* **B)** In `.mcp.json` at the project root, using environment variable expansion (e.g., `${JIRA_TOKEN}`) for authentication credentials.
* **C)** In a Markdown file inside `.claude/skills/jira.md`.
* **D)** In `.claude/rules/mcp.yaml` with path glob patterns.

---

### Question 5
An engineer needs to locate every invocation of a deprecated function `calculateTax()` across a codebase of 800 files. Which built-in tool should be selected first?

* **A)** `Read` on every file in sequence.
* **B)** `Grep` to search file contents for matching regex/text patterns across the project.
* **C)** `Glob` to list file paths in the src folder.
* **D)** `Edit` with string replacement targets.

---

### Question 6
Claude Code attempts to modify a configuration file using the built-in `Edit` tool, but the tool fails because the target text anchor appears multiple times in the file. What is the proper fallback workflow?

* **A)** Re-run `Edit` repeatedly with identical parameters.
* **B)** Force `tool_choice: "any"` on the next tool execution turn.
* **C)** Run `Grep` to overwrite the matching lines directly.
* **D)** Use `Read` to load the complete file contents into context, then use `Write` to update the file reliably.

---

### Question 7
To ensure an automated pipeline executes metadata extraction before performing any enrichment steps, how should `tool_choice` be set on the initial request?

* **A)** `tool_choice: "auto"`
* **B)** `tool_choice: "any"`
* **C)** Forced tool selection using `tool_choice: {"type": "tool", "name": "extract_metadata"}`.
* **D)** `tool_choice: "none"`

---

### Question 8
An MCP search tool executes a valid query for a customer record but finds no matching entries in the database. How should this result be communicated?

* **A)** Return a valid success response (`isError: false`) containing an empty result array, allowing the agent to distinguish empty matches from errors.
* **B)** Return `isError: true` with error category `"not_found"`.
* **C)** Throw a transient exception to trigger 3 automatic retries.
* **D)** Return an HTTP 404 error string as system prompt context.

---

### Question 9
An internal database MCP server contains 150 table schemas. To prevent an agent from wasting context tokens on exploratory schema inspection calls, how should schema metadata be exposed?

* **A)** Create 150 separate MCP tools for inspecting individual table schemas.
* **B)** Expose the full schema catalog as an MCP Resource for direct context reading.
* **C)** Embed the database schema in `~/.claude.json`.
* **D)** Use `Glob` to scan local migration scripts.

---

### Question 10
An engineer needs to locate all React TypeScript test files (`*.test.tsx`) scattered across deeply nested component subdirectories. Which tool is designed for this task?

* **A)** `Read`
* **B)** `Grep`
* **C)** `Glob` using the pattern `**/*.test.tsx`.
* **D)** `Bash` with `cat` commands.

---

## Topic 3: Claude Code Configuration & Workflows

### Question 1
A new team member cloned the repository, but Claude Code is not following the project's formatting standards. Diagnostics show the original author saved conventions in `~/.claude/CLAUDE.md`. How should this be resolved?

* **A)** Move project conventions to project-level `.claude/CLAUDE.md` or root `CLAUDE.md` in the repository so they are version-controlled.
* **B)** Instruct the team member to run `/memory` to load local environment settings.
* **C)** Embed conventions in `.mcp.json`.
* **D)** Add an `@import` line pointing to the author's home directory.

---

### Question 2
A project contains unit tests colocated next to component files across dozens of subdirectories. You want specific testing conventions to load automatically only when test files are being edited. What is the most maintainable setup?

* **A)** Manually place a `CLAUDE.md` file in every single component folder.
* **B)** Add testing rules to `~/.claude/CLAUDE.md`.
* **C)** Create a rule file in `.claude/rules/testing.md` with YAML frontmatter specifying `paths: ["**/*.test.*"]`.
* **D)** Create a slash command `/test-rules` that developers must run manually.

---

### Question 3
A developer created a custom skill in `.claude/skills/analyze-logs/SKILL.md` that generates 80KB of verbose output during execution. How can they prevent this output from cluttering the main conversation context?

* **A)** Set `context: fork` in the skill's YAML frontmatter to execute the skill in an isolated sub-agent context.
* **B)** Append `--resume` to the skill definition.
* **C)** Set `tool_choice: "none"` inside the frontmatter.
* **D)** Store the skill in `~/.claude/commands/`.

---

### Question 4
An engineering team needs to refactor an application's state management architecture across 35 files, requiring architectural analysis and evaluation of alternative library designs. Which mode should be selected?

* **A)** Direct execution mode, modifying files incrementally.
* **B)** CI/CD automated execution mode using `-p`.
* **C)** Direct execution mode with `context: fork` applied to all file tool calls.
* **D)** Plan mode, to safely explore codebase dependencies, evaluate architectural tradeoffs, and establish an execution strategy before making edits.

---

### Question 5
A GitHub Action step running `claude "Analyze pull request #15 for security flaws"` hangs indefinitely during CI execution. What CLI flag resolves this issue?

* **A)** `-p` (or `--print`) to run Claude Code in non-interactive mode, output results to stdout, and exit.
* **B)** `--batch-mode`
* **C)** `--headless`
* **D)** `--no-input`

---

### Question 6
An automated CI pipeline needs to parse Claude Code security findings and post them as inline PR review comments using a structured schema. Which CLI flags should be specified?

* **A)** `--resume` and `--json-schema`
* **B)** `-p` and `context: fork`
* **C)** `--output-format json` combined with `--json-schema` specifying the output schema.
* **D)** `--compact` and `--batch`

---

### Question 7
A root `CLAUDE.md` file has grown to over 1,500 lines, slowing down session initialization and exceeding context budgets. What modularization strategy should be applied?

* **A)** Delete historical rules and rely on model training defaults.
* **B)** Split instructions into topic-specific files inside `.claude/rules/` or reference external standard files using `@import` syntax.
* **C)** Move all project instructions to `~/.claude.json`.
* **D)** Compress the text using shorthand acronyms.

---

### Question 8
During an extended development session, Claude Code appears to be ignoring recently modified project rules. Which command should the developer execute to verify which memory files are currently active?

* **A)** `/compact`
* **B)** `/memory`
* **C)** `/resume`
* **D)** `/mcp`

---

### Question 9
When designing an automated migration script for an unfamiliar legacy database, how can a developer prevent Claude Code from making incorrect structural assumptions?

* **A)** Run direct execution without initial parameters.
* **B)** Pass all raw SQL schema dumps into a single prompt turn.
* **C)** Set `allowed-tools` to read-only permanently.
* **D)** Instruct Claude to use the "interview pattern"—asking clarifying questions to surface non-obvious constraints before implementing solutions.

---

### Question 10
When re-running an automated CI PR review after a developer pushes new commits, how should the prompt be structured to avoid posting duplicate feedback?

* **A)** Include prior review findings in context and explicitly instruct Claude to report only new or still-unaddressed issues.
* **B)** Omit previous review results completely from the prompt.
* **C)** Reset repository git history before running the review command.
* **D)** Force `tool_choice: "any"` to re-analyze all files.

---

## Topic 4: Prompt Engineering & Structured Output

### Question 1
An automated code review prompt produces high false positive rates by flagging minor formatting preferences as critical security bugs. How can prompt engineering improve review precision?

* **A)** Add generic guidance like "Be conservative and report only high-confidence findings."
* **B)** Increase `max_tokens` to give the model more reasoning space.
* **C)** Define explicit, categorical review criteria with concrete code examples distinguishing genuine security flaws from acceptable patterns.
* **D)** Switch `tool_choice` to `"any"`.

---

### Question 2
An application must extract structured metadata from unstructured financial documents without risk of JSON syntax parsing errors. What approach guarantees schema compliance?

* **A)** Define an extraction tool using `tool_use` with a strict JSON schema parameter definition.
* **B)** Request JSON format in plain text prose system instructions.
* **C)** Parse natural language responses using regular expressions.
* **D)** Process documents using the Message Batches API in plaintext mode.

---

### Question 3
Source documents processed by an extraction pipeline frequently lack fields such as `tax_id` or `discount_code`. How should the JSON schema be designed to avoid hallucinated values?

* **A)** Mark all schema properties as required string types.
* **B)** Define missing fields as optional or `nullable` in the JSON schema, instructing the model to output `null` when information is absent.
* **C)** Provide default dummy values like `"N/A"` for missing fields.
* **D)** Enforce `tool_choice: "any"` to force value generation.

---

### Question 4
An extraction output passes JSON schema syntax validation but fails semantic validation (e.g., individual item costs do not sum to `total_amount`). What is the most effective recovery loop?

* **A)** Re-send the identical original prompt unchanged up to 3 times.
* **B)** Route the document directly to manual human review without retrying.
* **C)** Strip out JSON schema parameter constraints and re-prompt in plain text.
* **D)** Issue a follow-up request containing the original document, the failed JSON output, and specific semantic validation error details to guide self-correction.

---

### Question 5
An enterprise needs to process 50,000 archived legal contracts overnight for non-urgent compliance auditing. Which API strategy optimizes cost?

* **A)** Synchronous real-time API calls across parallel worker threads.
* **B)** The Message Batches API, offering 50% cost savings for latency-tolerant workloads completed within a 24-hour window.
* **C)** Real-time Claude Code CLI processing using `-p`.
* **D)** Multi-turn interactive agentic loops executed via batch endpoints.

---

### Question 6
Why is it impossible to run an interactive, multi-turn tool-calling agentic loop directly within a single Message Batches API request?

* **A)** Message Batches API does not support `tool_use` parameter schemas.
* **B)** Message Batches API has a maximum limit of 5 requests per submission.
* **C)** Message Batches API does not support multi-turn tool execution within a single batch request (it cannot execute tools mid-request and return results for iteration).
* **D)** Message Batches API only accepts plaintext inputs.

---

### Question 7
A single-pass code review analyzing 16 modified files simultaneously yields superficial feedback and misses obvious cross-file data flow bugs. How should the review architecture be restructured?

* **A)** Restructure into a multi-pass architecture: execute focused per-file local analysis passes, followed by a separate integration pass examining cross-file data flow.
* **B)** Increase model `temperature` to 1.0.
* **C)** Concatenate all 16 files into a single plaintext prompt and disable tool calls.
* **D)** Automatically reject pull requests modifying more than 3 files.

---

### Question 8
System prompt guidelines alone fail to produce consistent formatting in generated bug reports. Which prompt engineering technique provides the highest leverage for output consistency?

* **A)** Repeat formatting instructions three times in the system prompt.
* **B)** Include 2–4 targeted few-shot examples demonstrating input code, reasoning steps, and exact desired output structure.
* **C)** Reduce `max_tokens` parameter limits.
* **D)** Omit system prompts and place all guidelines in user messages.

---

### Question 9
A structured extraction pipeline categorizes customer feedback into standard enum categories, but novel feedback types occasionally appear. How should the JSON schema enum property be structured?

* **A)** Restrict the enum strictly to fixed strings and reject unlisted entries.
* **B)** Use an untyped free-text string for all categories.
* **C)** Set `tool_choice: "none"`.
* **D)** Include standard enum values plus an `"other"` option paired with a nullable `category_detail` string field.

---

### Question 10
Why is an independent Claude instance more effective at reviewing generated code than having the same session review its own generated code?

* **A)** Independent instances bypass token rate limits.
* **B)** Independent instances process prompts faster.
* **C)** The generating session retains internal reasoning context, creating self-review bias and making it less likely to question its own decisions.
* **D)** Independent instances automatically set temperature to 0.0.

---

## Topic 5: Context Management & Reliability

### Question 1
During multi-turn customer support interactions, progressive context summarization often strips critical details like order numbers and exact monetary values. How should key transactional facts be managed?

* **A)** Summarize conversation context every 2 turns regardless of token usage.
* **B)** Extract exact transactional facts (order numbers, monetary amounts, verification status) into a persistent "case facts" block maintained in every prompt outside summarized history.
* **C)** Store all historical tool results in system prompt memory permanently.
* **D)** Replace exact monetary amounts with broad qualitative descriptions.

---

### Question 2
A customer expresses mild frustration regarding a delayed delivery, but the request is within standard automated policy guidelines. How should the resolution agent respond?

* **A)** Acknowledge the customer's frustration, offer clear autonomous resolution options within policy, and escalate only if policy gaps exist or the customer explicitly requests human intervention.
* **B)** Escalate immediately to a human supervisor due to negative sentiment detection.
* **C)** Pause interaction until sentiment scores normalize.
* **D)** Self-report low confidence to force an automated handoff.

---

### Question 3
In a multi-agent research pipeline, aggregating outputs from 8 search subagents into a single prompt causes the synthesis agent to omit key facts located in middle sections. What context structuring technique mitigates this "lost in the middle" effect?

* **A)** Concatenate all subagent outputs as continuous unformatted text.
* **B)** Randomize the order of subagent outputs on every execution turn.
* **C)** Truncate all subagent outputs to 100 words.
* **D)** Place key findings summaries at the beginning of the context, organize sections with explicit structural headers, and repeat critical synthesis instructions at the end.

---

### Question 4
A document analysis subagent encounters a network timeout while querying a remote database. How should this failure be reported to the coordinator agent to enable intelligent recovery?

* **A)** Return an empty success result set to mask the error.
* **B)** Raise an unhandled system exception to crash the coordinator.
* **C)** Return structured error context detailing failure category, query attempted, partial results gathered, and alternative queries to attempt.
* **D)** Return a generic string `"Error: search failed"`.

---

### Question 5
During an extended codebase refactoring session, Claude Code starts providing inconsistent recommendations due to context degradation. What pattern maintains context fidelity across turns?

* **A)** Execute `/compact` after every single tool call.
* **B)** Maintain a persistent scratchpad file (e.g., `scratchpad.md`) where key findings, dependencies, and architectural decisions are continuously updated and referenced.
* **C)** Restart the terminal every 5 turns.
* **D)** Maximize `max_tokens` parameter limits on all requests.

---

### Question 6
An enterprise data extraction pipeline reports 97% overall accuracy, but downstream users report frequent extraction errors on complex contract documents. How should human review routing be calibrated?

* **A)** Segment accuracy metrics by document type and field, using stratified random sampling on high-confidence extractions to detect novel error patterns.
* **B)** Rely exclusively on global aggregate accuracy metrics.
* **C)** Set high-confidence threshold cuts to 100% across all documents.
* **D)** Discontinue human review entirely for documents extracted via `tool_use`.

---

### Question 7
Two credible web sources analyzed by research subagents report conflicting statistics regarding market size ($12B vs $18B). How should the synthesis agent handle this in the final output?

* **A)** Average the two numbers together ($15B) and cite both sources.
* **B)** Arbitrarily choose the higher statistic and omit the lower one.
* **C)** Preserve source attribution, explicitly annotating the discrepancy and presenting both statistics with their respective publication dates and source contexts.
* **D)** Omit market size statistics completely from the report.

---

### Question 8
A `get_customer` lookup by last name returns 4 matching customer account records. What is the correct agent action?

* **A)** Select the first record automatically to maintain response speed.
* **B)** Escalate immediately to human support without interacting with the customer.
* **C)** Process operations for all 4 profiles simultaneously.
* **D)** Ask the user for additional unique identifiers (e.g., email address, account ID) to disambiguate before taking further action.

---

### Question 9
An order lookup tool returns a raw payload containing 75 fields, but only 5 fields are relevant to return processing. What context management strategy prevents context clutter?

* **A)** Append full raw JSON payloads into conversation history on every call.
* **B)** Filter and trim tool outputs to retain only task-relevant fields before appending results to conversation context.
* **C)** Convert JSON payloads into binary strings.
* **D)** Delete tool execution history completely after each turn.

---

### Question 10
Financial reports synthesized from multi-year sources contain contradicting growth figures due to differing data collection years. What context requirement prevents misinterpretation?

* **A)** Mandate that subagents include publication and data collection dates in structured findings, allowing synthesis to evaluate temporal progression rather than treating differences as contradictions.
* **B)** Exclude all historical data older than 6 months.
* **C)** Convert all dates into Unix timestamps.
* **D)** Force the synthesis agent to use `tool_choice: "any"`.

---

# Answer Key & Explanations

## Topic 1: Agentic Architecture & Orchestration

* **1. Correct Answer: B**
  * **Explanation:** The agentic loop control flow must inspect `stop_reason`. When `stop_reason` is `"tool_use"`, the application executes the requested tool(s) and appends results back to context; when `stop_reason` is `"end_turn"`, the loop completes and outputs the final response.
* **2. Correct Answer: C**
  * **Explanation:** Subagents operate with isolated context windows and do not automatically inherit conversation history or system prompts from the coordinator; any required background context must be explicitly passed in the subagent's prompt.
* **3. Correct Answer: A**
  * **Explanation:** Spawning parallel subagents is achieved by emitting multiple `Task` tool calls within a single assistant turn response, allowing concurrent execution and reducing overall system latency.
* **4. Correct Answer: D**
  * **Explanation:** When strict compliance or financial safety requires specific step ordering (such as verifying identity before refunding), programmatic prerequisite checks/hooks provide deterministic guarantees that probabilistic prompt guidance cannot.
* **5. Correct Answer: B**
  * **Explanation:** Tool call interception hooks run deterministically in application code prior to tool execution, evaluating outgoing parameters to block policy-violating actions reliably.
* **6. Correct Answer: B**
  * **Explanation:** To fix overly narrow task decomposition, the coordinator prompt must instruct the agent to evaluate synthesis outputs against query scope and iteratively re-delegate targeted sub-queries until research coverage criteria are satisfied.
* **7. Correct Answer: B**
  * **Explanation:** `fork_session` creates independent, parallel exploration branches from a shared baseline context, allowing developers to compare divergent implementations without corrupting the baseline session state.
* **8. Correct Answer: A**
  * **Explanation:** `Post ToolUse` hooks intercept tool result payloads before they enter conversation context, providing a deterministic layer for data normalization (e.g., standardizing timestamps) across heterogeneous MCP sources.
* **9. Correct Answer: B**
  * **Explanation:** Complex, unformatted legacy tasks require dynamic adaptive decomposition: mapping module structure, identifying high-impact entry points, formulating a prioritized plan, and executing changes incrementally with test verification.
* **10. Correct Answer: C**
  * **Explanation:** Using structured schemas (JSON/YAML) separates core facts from metadata (URLs, document titles, dates), ensuring that downstream subagents maintain accurate information provenance through summarization passes.

---

## Topic 2: Tool Design & MCP Integration

* **1. Correct Answer: B**
  * **Explanation:** Ambiguous or overlapping tool selection is primarily caused by minimal descriptions. Expanding tool descriptions to explicitly define functional boundaries, expected inputs, example queries, and distinctions from similar tools resolves misrouting.
* **2. Correct Answer: C**
  * **Explanation:** Returning structured error metadata (`errorCategory`, `isRetryable: true`, descriptions) via the MCP `isError` pattern enables the agent to make intelligent recovery decisions (e.g., retrying transient failures vs informing users of policy errors).
* **3. Correct Answer: A**
  * **Explanation:** Scoped tool access restricts each agent's tool set strictly to those necessary for its designated role, preventing agents from misusing tools outside their domain (e.g., synthesis agents attempting web searches).
* **4. Correct Answer: B**
  * **Explanation:** Project-scoped MCP configuration belongs in `.mcp.json` at the project root using environment variable expansion (e.g., `${JIRA_TOKEN}`), ensuring shared team availability via version control without committing secret keys.
* **5. Correct Answer: B**
  * **Explanation:** `Grep` is purpose-built for searching code content across a codebase (such as function call signatures or error strings).
* **6. Correct Answer: D**
  * **Explanation:** When `Edit` fails due to non-unique target anchor text in a file, the reliable fallback workflow is using `Read` to load the full file content, followed by `Write` to overwrite the complete file accurately.
* **7. Correct Answer: C**
  * **Explanation:** Forced tool selection using `tool_choice: {"type": "tool", "name": "extract_metadata"}` forces the model to call the named tool on that specific turn before processing follow-up turns.
* **8. Correct Answer: A**
  * **Explanation:** Successful queries that yield zero matching database records should return a success response (`isError: false`) with an empty result set, allowing the model to correctly distinguish valid empty matches from system errors.
* **9. Correct Answer: B**
  * **Explanation:** MCP Resources allow backend systems to expose content catalogs and structural metadata (like database schemas) directly for context reading, eliminating the need for exploratory tool call loops.
* **10. Correct Answer: C**
  * **Explanation:** `Glob` is the built-in tool designed for file path pattern matching (e.g., finding files by extension or name patterns across directory hierarchies).

---

## Topic 3: Claude Code Configuration & Workflows

* **1. Correct Answer: A**
  * **Explanation:** Project-level `.claude/CLAUDE.md` or root `CLAUDE.md` is version-controlled and shared across all repository contributors, whereas `~/.claude/CLAUDE.md` is user-scoped and private to an individual developer's machine.
* **2. Correct Answer: C**
  * **Explanation:** `.claude/rules/` files with YAML frontmatter `paths` glob patterns (e.g., `**/*.test.*`) dynamically load rules only when editing matching file paths across the codebase.
* **3. Correct Answer: A**
  * **Explanation:** Configuring `context: fork` in a skill's YAML frontmatter executes the skill inside an isolated sub-agent context, preventing verbose exploration output from polluting the main session conversation window.
* **4. Correct Answer: D**
  * **Explanation:** Plan mode is designed for complex tasks involving architectural decisions, multi-file modifications, and structural exploration, allowing safe design and planning before committing code changes.
* **5. Correct Answer: A**
  * **Explanation:** The `-p` (or `--print`) CLI flag runs Claude Code in non-interactive mode, rendering prompt responses directly to stdout and exiting without hanging for interactive user input.
* **6. Correct Answer: C**
  * **Explanation:** Combining `--output-format json` with `--json-schema` enforces structured, machine-parseable JSON output in non-interactive CI/CD workflows for automated PR commenting.
* **7. Correct Answer: B**
  * **Explanation:** Splitting monolithic `CLAUDE.md` files into focused topic rules in `.claude/rules/` or using `@import` syntax preserves modularity and minimizes unnecessary token usage.
* **8. Correct Answer: B**
  * **Explanation:** The `/memory` command displays all currently active memory and rule files loaded in the session context, helping diagnose configuration hierarchy issues.
* **9. Correct Answer: D**
  * **Explanation:** The "interview pattern" instructs Claude to ask clarifying questions to surface non-obvious requirements and constraints before implementing code solutions in unfamiliar or complex domains.
* **10. Correct Answer: A**
  * **Explanation:** Including prior review comments in context and explicitly instructing Claude to report only new or remaining unaddressed issues prevents duplicate feedback on iterative PR commits.

---

## Topic 4: Prompt Engineering & Structured Output

* **1. Correct Answer: C**
  * **Explanation:** Vague guidelines like "be conservative" fail to reduce false positives. Providing explicit categorical criteria with concrete code examples clearly defines acceptable versus bug patterns.
* **2. Correct Answer: A**
  * **Explanation:** Using `tool_use` with strict JSON schema parameter definitions forces model output to conform to schema structures, eliminating JSON syntax and formatting errors.
* **3. Correct Answer: B**
  * **Explanation:** Marking missing or optional fields as `nullable` in JSON schemas—and instructing the model to return `null` when data is absent—prevents the model from fabricating values to fulfill required schema parameters.
* **4. Correct Answer: D**
  * **Explanation:** For semantic validation errors (where syntax is valid but business math/logic fails), feeding the original input, the failed JSON output, and exact validation error messages back to the model guides effective self-correction.
* **5. Correct Answer: B**
  * **Explanation:** The Message Batches API offers a 50% cost reduction for latency-tolerant, non-blocking batch workloads processed within a 24-hour SLA window.
* **6. Correct Answer: C**
  * **Explanation:** The Message Batches API process consists of single request-response pairs and does not support multi-turn tool execution loops mid-request.
* **7. Correct Answer: A**
  * **Explanation:** Splitting multi-file code reviews into a multi-pass architecture (per-file local analysis passes followed by a cross-file integration pass) eliminates attention dilution and contradictory findings.
* **8. Correct Answer: B**
  * **Explanation:** Providing 2–4 targeted few-shot examples demonstrating input, reasoning steps, and desired output formatting is the most effective technique for achieving consistent output structures.
* **9. Correct Answer: D**
  * **Explanation:** Structuring schema enums with standard values plus an `"other"` option paired with a nullable `category_detail` string field allows clean categorization while gracefully handling novel categories.
* **10. Correct Answer: C**
  * **Explanation:** Self-review within the generating session suffers from confirmation bias because the model retains its original reasoning context; an independent review instance without prior context provides objective code evaluation.

---

## Topic 5: Context Management & Reliability

* **1. Correct Answer: B**
  * **Explanation:** Persisting exact key transactional facts (e.g., account IDs, order numbers, dollar amounts) in a dedicated "case facts" context block outside summarized history prevents information loss during progressive conversation compaction.
* **2. Correct Answer: A**
  * **Explanation:** Sentiment alone is an unreliable proxy for complexity. Agents should acknowledge frustration, offer clear policy-compliant autonomous options, and escalate only when policy gaps occur or human handoff is explicitly requested.
* **3. Correct Answer: D**
  * **Explanation:** Mitigating the "lost in the middle" effect involves placing key summary data at the start of context, organizing middle sections with explicit structural headers, and reiterating critical instructions near the end.
* **4. Correct Answer: C**
  * **Explanation:** Subagents should return structured error context (failure type, query attempted, partial findings, alternative query ideas) so the coordinator agent can make intelligent recovery or re-routing decisions.
* **5. Correct Answer: B**
  * **Explanation:** Maintaining a persistent scratchpad file (`scratchpad.md`) provides a clean, continuous record of core findings and decisions that survives context compaction during long codebase exploration sessions.
* **6. Correct Answer: A**
  * **Explanation:** Evaluating global aggregate accuracy masks segment-specific failures. Accuracy must be segmented by document type and field, combined with stratified random sampling of high-confidence extractions to detect novel error patterns.
* **7. Correct Answer: C**
  * **Explanation:** When handling conflicting statistics from credible sources, the synthesis agent must preserve provenance, explicitly annotating discrepancies and citing both sources along with collection/publication dates.
* **8. Correct Answer: D**
  * **Explanation:** When tool lookups return ambiguous multiple matches, the agent must prompt the user for additional unique identifiers to disambiguate before taking further action.
* **9. Correct Answer: B**
  * **Explanation:** Trimming verbose tool payloads to retain only fields relevant to the task prevents context window bloat and reduces noise during multi-turn interactions.
* **10. Correct Answer: A**
  * **Explanation:** Mandating that subagents capture publication and data collection dates in structured outputs enables downstream synthesis agents to contextualize temporal progression accurately rather than misinterpreting differences as contradictions.
