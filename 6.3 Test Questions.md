--- QUIZ ---

Domain: 1 Agentic Architecture & Orchestration  with Scenario 1. Customer Support Resolution Agent

1. Your customer support resolution agent built with the Claude Agent SDK handles high-ambiguity requests like refund disputes. To maintain deterministic control over financial policy, you must ensure that the `process_refund` tool cannot be executed unless `get_customer` has already successfully verified the user's account ID in the conversation. Which implementation pattern reliably enforces this constraint?

* A) Instruct Claude in the system prompt to always call `get_customer` and verify account details prior to emitting a tool call for `process_refund`.
* B) Add a few-shot message sequence showing the model calling `get_customer`, receiving the verified user ID, and subsequently calling `process_refund`.
* C) Implement a programmatic prerequisite hook in the SDK that checks conversation state and blocks `process_refund` tool execution until `get_customer` returns a verified customer ID.


* D) Set `tool_choice: {"type": "tool", "name": "get_customer"}` on every user turn until the system receives valid account details from the user.

2. During an agentic execution loop handling a return request, the model receives a response from the `lookup_order` tool. What criteria should your execution control loop inspect to determine whether the agent should execute another tool or present a final response to the user?

* A) Parse the assistant's output string for natural language completion signals such as "I have completed your request."
* C) Track the total iteration count against a fixed maximum cap, terminating the loop whenever the cap is reached regardless of model state.
* B) Inspect the `stop_reason` field returned in the API response, continuing the loop if `stop_reason` is `"tool_use"` and exiting when it is `"end_turn"`.


* D) Check whether the assistant content block contains plain text narrative rather than JSON tool call signatures.

3. A customer submits a multi-issue ticket requesting an account email change, a return status check, and a shipping fee refund. How should the agent architect its investigation to maximize first-contact resolution while avoiding context pollution?

* A) Execute all backend lookup operations sequentially in a single single-threaded context thread without saving intermediate state.
* B) Decompose the multi-concern request into individual sub-items, execute parallel background lookup calls using shared context, and synthesize a single consolidated resolution.


* C) Request that the user submit three separate support tickets so that distinct agent instances can process each issue independently.
* D) Immediately escalate the ticket to a human agent, as handling multiple distinct domain issues in a single session violates tool safety principles.

4. You need to implement a compliance rule that automatically blocks any refund request over $500 and redirects the user to human escalation. Which SDK architecture guarantees that this financial safety guardrail is enforced without relying on probabilistic model compliance?

* A) Use a tool call interception hook (or pre-tool execution hook) that evaluates the target tool's payload arguments, cancels execution if `amount > 500`, and triggers the human escalation path.


* B) Append an explicit rule in the system prompt: "If the calculated refund exceeds $500, do not call process_refund; instead call escalate_to_human."
* C) Include 10 few-shot examples demonstrating refund requests above $500 being routed to `escalate_to_human`.
* D) Adjust the system prompt temperature to `0.0` to ensure strict compliance with written policy guidelines.

5. An agent handling a complex billing dispute needs to escalate the ticket to a human support agent who does not have access to the full LLM conversation transcript. What handoff mechanism provides the most efficient transition?

* A) Pass the raw JSON transcript containing all raw API request/response turns directly to the human agent portal.
* B) Compile a structured handoff summary containing the customer ID, verified root cause, refund status, and specific recommended action before triggering `escalate_to_human`.


* C) Prompt the customer to repeat their full issue description to the human support agent once connected.
* D) Send only the original user prompt text to the human escalation queue to minimize payload size.

6. An agentic support workflow encounters a scenario where a tool returns a non-retryable business logic error (e.g., account frozen due to policy breach). How should the control loop handle context and tool feedback?

* A) Suppress the tool result, delete the tool call from history, and re-prompt the model to guess alternative inputs.
* B) Append the structured tool output—including the explicit error details—back into the conversation context so Claude can evaluate the failure and determine the next logical action.


* C) Immediately throw a runtime system exception and abort the user session without generating an assistant response.
* D) Reset the entire conversation context to zero tokens and start a brand-new turn with generic messaging.

7. You are implementing custom SDK hooks to handle data returned from legacy enterprise backend tools that return heterogeneous date formats and numeric status codes. Which hook design optimizes context efficiency for Claude?

* A) Implement a `Post ToolUse` hook that normalizes timestamps into standard ISO 8601 strings and decodes numeric codes into descriptive status strings before the tool result is injected into context.


* B) Use a `Pre ToolUse` hook to modify system instructions, adding explicit date format parsing rules dynamically before every tool call.
* C) Allow raw legacy payloads to enter context unaltered, relying on few-shot prompt examples to teach Claude how to parse disparate formats.
* D) Intercept the assistant message and write raw legacy payloads to a local disk file, passing only the file path to Claude.

8. In an agentic loop, what is the primary risk of relying solely on an arbitrary maximum iteration count cap (e.g., stopping after 5 turns) as the primary completion indicator?

* A) It causes the API to throw a schema validation error when `stop_reason` is evaluated.
* B) It may prematurely truncate valid multi-step investigations or force the loop to continue unnecessarily when tasks finish early.


* C) It causes Claude to forget tool definitions provided in the system prompt.
* D) It disables the model's ability to issue structured JSON tool calls.

9. When designing an agentic customer resolution system, why is model-driven tool chaining preferred over rigid, pre-configured decision trees for high-ambiguity requests?

* A) Model-driven tool chaining eliminates the requirement for schema validation on backend API parameters.
* B) Rigid decision trees cannot dynamically adapt tool selection based on unexpected intermediate tool findings or nuanced user input.


* C) Pre-configured decision trees increase token consumption by 300% on every conversation turn.
* D) Model-driven tool chaining guarantees 100% deterministic code execution across all system state boundaries.

10. An agent handling returns needs to invoke `lookup_order` followed by `process_refund`. If the system prompt contains keyword-sensitive phrasing that inadvertently biases tool selection toward `process_refund` prematurely, what is the best architectural correction?

* A) Remove all tool descriptions from the API definition to avoid keyword collisions.
* B) Enforce programmatic prerequisite gating in code while refining system prompt instructions to maintain unambiguous operational boundaries.


* C) Hardcode all tool arguments directly into system environment variables.
* D) Force `tool_choice: "none"` on all initial interaction turns.

11. When resuming a customer support agent session that was paused mid-investigation, what strategy ensures context validity while avoiding token bloat?

* A) Start a fresh session with an injected structured summary of prior facts if past tool results are stale or verbose.


* B) Reload the full raw payload of every historical backend database call into active context.
* C) Re-execute all historical tool calls sequentially to rebuild state from scratch.
* D) Send only the final assistant text turn without any preceding account or order context.

12. Your customer resolution agent requires user verification before issuing refunds. What is the fundamental limitation of using prompt instructions alone (e.g., "Always verify identity first") for this policy?

* A) Prompt instructions are not supported when using the Claude Agent SDK.
* B) LLM compliance with prompt-only instructions is probabilistic and carries a non-zero failure rate in production.


* C) Prompt instructions prevent the model from parsing JSON tool outputs correctly.
* D) System prompts increase system latency by forcing sequential batch processing.

13. How should an agentic loop handle an unexpected transient HTTP 503 timeout returned by an MCP tool?

* A) Return a generic success message to the user stating the operation completed.
* B) Catch the error, tag it with structured metadata indicating it is transient and retryable, and allow the agent or control loop to perform a controlled retry.


* C) Permanently disable the tool for all future turns in the user session.
* D) Delete the customer's account context from memory and end the session.

14. You want to evaluate whether an agent's multi-tool plan is progressing correctly. What agentic loop state component provides the authoritative reasoning context for Claude's next move?

* A) The system prompt text isolated from conversation history.
* B) The appended sequence of user messages, assistant `tool_use` outputs, and corresponding `tool_result` content blocks in the message history.


* C) The local file system environment variables configured in the runner application.
* D) The raw HTTP header logs of the API gateway.

15. An agent needs to handle a policy exception where a loyal customer requests a fee waiver that standard rules forbid. What is the correct orchestration behavior?

* A) Fabricate a false order status code to force the standard tool to execute successfully.
* B) Recognize the policy boundary, log the exception context, and trigger a structured escalation protocol to human supervisors.


* C) Loop infinitely while retrying the standard tool call until it succeeds.
* D) Inform the customer that the system has crashed and terminate the session.

---

Domain: 1 Agentic Architecture & Orchestration  with Scenario 2. Multi-Agent Research System

1. You are designing a multi-agent research system using the Claude Agent SDK. A coordinator agent delegates tasks to specialized subagents (web search, document analysis, report synthesis). Which architectural principle governs context inheritance between the coordinator and subagents?

* A) Subagents automatically inherit the full, active context history of the coordinator agent upon instantiation.
* B) Subagents operate with isolated context windows and must be explicitly provided with all relevant background context in their invocation prompt.


* C) Subagents share a synchronized, real-time shared memory buffer updated automatically by the SDK core runner.
* D) Context is inherited only if subagents share the exact same system prompt as the coordinator agent.

2. To allow a coordinator agent to spawn specialized subagents dynamically in the Claude Agent SDK, what configuration requirement must be met in the coordinator's definition?

* A) The coordinator's `allowedTools` list must explicitly include the `"Task"` tool.


* B) The coordinator must be executed in non-interactive batch mode (`-p`).
* C) Subagents must be registered as local environment variables prior to system execution.
* D) The system prompt must be saved in a `.mcp.json` file located in the workspace root directory.

3. A multi-agent research system on "The Economic Impact of AI" produces reports that focus exclusively on tech industry employment, completely ignoring healthcare, manufacturing, and finance. Logs show the coordinator issued three subagent tasks: "AI in software dev", "AI in IT hiring", and "Tech sector wages". What is the root cause?

* A) The synthesis subagent failed to execute its deduplication pass.
* B) The coordinator agent performed overly narrow task decomposition, failing to partition the research scope across all primary economic sectors.


* C) The search subagent's web search API rate limit was exceeded during execution.
* D) The subagents were executed in parallel rather than sequentially.

4. How should a coordinator agent orchestrate multiple subagents when deep research requires simultaneous web searching and local document analysis?

* A) Force the agents to execute strictly sequentially over multiple user turns to prevent race conditions.
* B) Emit multiple `Task` tool calls in a single coordinator response turn to trigger parallel subagent execution.


* C) Pass all document file contents directly into the web search tool's API payload.
* D) Merge all specialized subagent prompt instructions into a single monolithic system prompt.

5. The synthesis subagent needs to write a comprehensive report combining output from the search subagent and document analysis subagent. What is the most reliable pattern for context passing from coordinator to synthesis agent?

* A) Instruct the synthesis agent to query the coordinator's context history using a custom string retrieval function.
* B) Provide a file path reference to raw intermediate tool output logs stored on the local disk.
* C) Inject complete structured findings (including source metadata and claims) directly into the synthesis subagent's prompt payload.


* D) Use `tool_choice: "auto"` to let the synthesis agent guess prior agent outputs based on task titles.

6. During a multi-agent research workflow, the document analysis subagent encounters a corrupted PDF file and fails. What error handling strategy enables the coordinator to make an intelligent recovery decision?

* A) Return a generic string "Task Failed" from the subagent to keep context small.
* B) Suppress the failure completely and present empty research findings as a successful run.
* C) Return structured error context detailing the failure type, attempted file operations, partial extractions, and potential fallback sources to the coordinator.


* D) Immediately crash the entire coordinator process and terminate the research pipeline.

7. You observe that a synthesis subagent regularly sends short follow-up verification requests back to the coordinator to double-check simple facts (e.g., publication dates, author names), causing excessive latency. How should you optimize this architecture?

* A) Grant the synthesis agent access to the full web search tool suite with unrestricted execution capabilities.
* B) Provide the synthesis subagent with a scoped `verify_fact` tool for lightweight lookups, reserving complex research delegation for the coordinator.


* C) Hardcode all factual facts directly into the coordinator's system prompt before running the pipeline.
* D) Disable factual verification entirely in the synthesis agent's quality assurance rubrics.

8. When designing a hub-and-spoke multi-agent system, why is direct, unmonitored communication between individual subagents considered an architectural anti-pattern?

* A) It violates HTTP 1.1 protocol constraints within the Claude Agent SDK.
* B) It undermines central observability, consistent error handling, and controlled information routing managed by the coordinator.


* C) It causes the Claude API to reject tool schemas due to circular JSON dependency checks.
* D) It reduces context token usage below required minimum operating thresholds.

9. A research system needs to compare two divergent economic projection models starting from a shared baseline dataset. Which session management mechanism in the Claude Agent SDK supports this workflow?

* A) Executing `/memory` compact on the primary coordinator session.
* B) Using `fork_session` (or context forking) to create independent exploration branches from the shared baseline analysis context.


* C) Running the entire pipeline inside a single user-scoped `~/.claude/CLAUDE.md` file.
* D) Setting `max_tokens` to `100` on the model initialization call.

10. In a multi-agent research pipeline, how should the coordinator evaluate whether synthesized report output meets target research depth before finalizing the task?

* A) Check if the raw character length of the generated report exceeds 10,000 characters.
* B) Implement an iterative refinement loop where the coordinator evaluates synthesis output against coverage rubrics, re-delegating targeted queries to subagents if gaps exist.


* C) Count the exact number of JSON punctuation marks present in the synthesis block.
* D) Monitor the local CPU usage of the runner node hosting the Agent SDK.

11. To preserve source attribution when passing research findings from analysis subagents to the synthesis subagent, what data structure formatting pattern should be enforced?

* A) Convert all text into unformatted plain prose paragraphs without metadata.
* B) Use structured data formats (e.g., JSON or explicit XML tags) separating claims, evidence excerpts, and source metadata (URLs, page numbers, dates).


* C) Encrypt all citation metadata using base64 string encoding.
* D) Strip all URL parameters and source names from intermediate findings to conserve context tokens.

12. Why is it advantageous to define subagent system prompts around clear high-level goals and quality criteria rather than step-by-step procedural instructions?

* A) Goal-oriented prompts enable subagents to dynamically adapt their tool execution paths based on intermediate findings during research.


* B) Procedural instructions are ignored by Claude models when tools are present.
* C) Goal-oriented prompts remove the requirement for JSON schema validation on tool calls.
* D) Procedural instructions cause API rate-limiting errors on large context requests.

13. An analysis subagent experiences a transient network drop while querying an internal database. Where should local error recovery be attempted first?

* A) Within the subagent itself using retry logic before propagating unresolvable failures to the coordinator.


* B) Directly by the user through a manual CLI prompt re-entry.
* C) At the top-level OS process supervisor level by restarting the virtual machine.
* D) Inside the report rendering component after synthesis is complete.

14. What design pattern prevents subagents from attempting actions outside their assigned functional domain (e.g., a report rendering agent trying to issue web search queries)?

* A) Adding a warning note in the user prompt.
* B) Applying scoped tool access in each subagent's definition, restricting available tools exclusively to those required for its specific role.


* C) Setting temperature to `1.0` across all subagent system configurations.
* D) Running all subagents in a single sequential thread without tool declarations.

15. When a research system is tasked with producing an exhaustive analysis, what coordinator task decomposition strategy avoids missing critical sub-topics?

* A) Decomposing tasks strictly by arbitrary chunk length (e.g., "Analyze first 100 lines").
* B) Partitioning research scope by distinct subtopics, source types, or perspectives, and validating coverage completeness prior to synthesis.


* C) Sending the exact same prompt to 10 identical subagents without task customization.
* D) Disabling the coordinator agent and letting subagents self-select tasks autonomously.

---

Domain: 2 Tool Design & MCP Integration  with Scenario 1. Customer Support Resolution Agent

1. Production logs show that your customer support agent frequently calls `get_customer` when users ask about order status (e.g., "Where is order #98765?"), instead of calling `lookup_order`. Both tools currently have minimal descriptions ("Retrieves customer info" / "Retrieves order details"). What is the most effective first step to fix this misrouting issue?

* A) Implement a custom keyword-parsing router in Python to bypass model tool selection entirely.
* B) Expand each tool's description to include precise functional boundaries, expected input formats, example queries, and explicit distinctions from similar tools.


* C) Consolidate both tools into a single mega-tool named `do_everything_tool`.
* D) Hardcode `tool_choice: {"type": "tool", "name": "lookup_order"}` for all customer messages containing digits.

2. Your support agent uses custom Model Context Protocol (MCP) tools connected to backend APIs. When a backend database query fails due to an invalid customer ID format, how should the MCP tool structure its response back to the agent?

* A) Return a plain string `"HTTP 500 Internal Server Error"` with no added context.
* B) Throw an unhandled code exception to terminate the agent process immediately.
* C) Set `isError: true` in the MCP response and return structured JSON metadata including `errorCategory: "validation"`, `isRetryable: false`, and a clear description.


* D) Return `isError: false` with an empty data payload to trick the agent into assuming the query succeeded.

3. An agent is provided with access to 22 different MCP tools spanning billing, returns, user profiles, marketing, and inventory. Developers notice tool selection accuracy has dropped significantly. What architectural change addresses this degradation?

* A) Increase the system prompt max token limit to maximum capacity.
* B) Scope tool availability down to a focused set of 4–5 core tools relevant to the agent's specific role, delegating non-core tasks through specialized subagents.


* C) Rename all tools using random numerical identifiers like `tool_1`, `tool_2`, etc.
* D) Instruct Claude in the prompt to try calling every single tool sequentially until one works.

4. You need to ensure that an agent extracting support ticket metadata ALWAYS invokes the `extract_ticket_details` tool first on the incoming message before taking any other action. Which API setting guarantees this behavior on the initial turn?

* A) `tool_choice: "auto"`
* B) `tool_choice: "any"`
* C) `tool_choice: {"type": "tool", "name": "extract_ticket_details"}`

* D) `tool_choice: "none"`

5. A support team provides two tools: `analyze_content` and `analyze_document`. Agents repeatedly mix up when to use each because the tool descriptions are functionally ambiguous. What tool design refactoring resolves this overlap?

* A) Delete both tools and force the agent to write custom Python scripts.
* B) Rename and redefine the tools with explicit input contracts and domain boundaries (e.g., `extract_web_results` vs `parse_pdf_attachment`).


* C) Instruct the model to flip a coin whenever it needs content analysis.
* D) Add a 200-line warning paragraph to the system prompt telling Claude not to make mistakes.

6. An MCP server tool returns business rule failure metadata when a refund is rejected (e.g., `policy_violation: "Item outside 30-day window"`). How does returning structured error metadata benefit the support agent's context reasoning?

* A) It allows the agent to clearly explain the specific business rationale to the customer rather than making wasteful retry attempts.


* B) It causes the Claude API to automatically credit the customer's credit card.
* C) It forces the agent to retry the tool call 10 times in a row.
* D) It hides the failure details from system monitoring logs.

7. What is the fundamental difference in purpose between an MCP Tool and an MCP Resource within a system architecture?

* A) Tools execute active side-effect operations or parameterized functions, whereas Resources expose passive readable data catalogs or context items.


* B) Tools are stored in user directories, while Resources can only be stored in cloud databases.
* C) Tools are used by human developers, while Resources are used only by LLMs.
* D) Tools are written in C++, while Resources must be written in HTML.

8. Why should transient API errors (e.g., HTTP 504 Gateway Timeout) returned by an MCP tool be explicitly tagged with `isRetryable: true` in the error response payload?

* A) To signal to the agent/runner that re-executing the operation with the same parameters may succeed, preventing unnecessary task abandonment.


* B) To force the runner to restart the operating system kernel.
* C) To instruct Claude to rewrite the MCP server source code automatically.
* D) To convert the tool output into structured markdown tables.

9. In a scenario where an agent needs to perform structured data extraction, setting `tool_choice: "any"` guarantees which model behavior?

* A) The model will select tool calls randomly from available options.
* B) The model is forced to call at least one of the available tools rather than responding with conversational text.


* C) The model will execute all defined tools simultaneously.
* D) The model will exit the agentic loop immediately without calling tools.

10. An agent developer notices that system prompt instructions containing phrases like "Search the order database for recent shipments" cause Claude to execute `db_search` even when a dedicated `get_shipping_status` tool exists. What tool design issue is present?

* A) The API runner is ignoring tool parameter schemas.
* B) Keyword-sensitive system prompt wording is overriding well-written tool descriptions due to unintended phrase matching.


* C) The model lacks context token capacity to read tool definitions.
* D) The `db_search` tool is missing an `isError` flag.

11. When configuring an agent tool that queries backend customer records, why is it vital to include edge cases and boundary conditions in the tool's parameter descriptions?

* A) To guide the model on how to handle ambiguous, missing, or multi-match input values gracefully before invoking the backend service.


* B) To reduce the latency of backend SQL queries.
* C) To eliminate the need for schema parameters in the tool definition.
* D) To force the API to return responses in XML format.

12. Your customer support agent needs access to a tool that returns order details containing 50+ database columns. Most of these fields are irrelevant to support inquiries and consume context rapidly. How should the tool output interface be redesigned?

* A) Return the raw SQL dump as an unformatted string payload.
* B) Filter and project tool results to include only relevant key fields (e.g., order ID, items, status, tracking) before returning data to context.


* C) Encrypt the JSON response payload.
* D) Store the order details in local temporary memory without passing any text to Claude.

13. When an MCP backend service encounters a authorization failure (e.g., user token lacks refund privileges), how should the tool signal this permission error?

* A) Return `isError: true` with `errorCategory: "permission"` and `isRetryable: false`, enabling the agent to route to escalation or request authentication.


* B) Return an empty string payload and mark the call as successful.
* C) Retry the tool call repeatedly until permissions change dynamically.
* D) Crash the application server host process.

14. Why is splitting a monolithic `manage_account` tool into smaller, discrete tools (`update_email`, `change_address`, `view_billing`) considered an architectural best practice?

* A) Distinct, purpose-built tools provide tight input schemas, clearer descriptions, and reduced ambiguity in tool selection.


* B) Monolithic tools are prohibited by the Claude Agent SDK schema validator.
* C) Smaller tools decrease backend API throughput capacity.
* D) It allows the model to bypass parameter validation checks completely.

15. When using forced tool selection (`tool_choice: {"type": "tool", "name": "..."}`), what happens on the subsequent turn after the tool result is injected into context?

* A) The model remains permanently locked into forcing that same tool.
* B) `tool_choice` typically reverts to default (`"auto"`), allowing the model to freely reason on the next action using the tool output.


* C) The conversation session is automatically reset to zero messages.
* D) The system raises a schema error unless `tool_choice` is set to `"none"`.

---

Domain: 2 Tool Design & MCP Integration  with Scenario 2. Developer Productivity with Claude

1. An engineer using Claude Code wants to find every location in a large codebase where a legacy function `calculate_tax()` is called across 200+ files. Which built-in tool is designed specifically for searching file contents by pattern?

* A) `Glob`
* B) `Grep`

* C) `Read`
* D) `Edit`

2. A developer needs to locate all React TypeScript test files matching the pattern `**/*.test.tsx` distributed throughout a nested repository directory structure. Which built-in tool should be selected?

* A) `Grep`
* B) `Glob`

* C) `Write`
* D) `Bash`

3. During a automated refactoring script, Claude attempts to modify a source file using the `Edit` tool, but the modification fails because the anchor search text matches multiple identical lines in the file. What is the standard fallback pattern?

* A) Abort the operation and delete the source file.
* B) Execute `Read` to load the full file contents into context, followed by `Write` to overwrite the full file cleanly.


* C) Run `Grep` repeatedly until the file text changes automatically.
* D) Call `Bash` with `rm -rf` commands.

4. Your team wants to integrate an external Jira issue tracking server into Claude Code so all developers on the project share the same integration automatically when cloning the repository. Where should this MCP server configuration be placed?

* A) In each developer's personal user file `~/.claude.json`.
* B) In the project-scoped `.mcp.json` file committed to the repository root directory.


* C) In the operating system environment variables file `/etc/environment`.
* D) In the global git configuration file `~/.gitconfig`.

5. In a project-scoped `.mcp.json` file, how should sensitive authentication credentials (such as GitHub personal access tokens) be handled to prevent committing secrets to version control?

* A) Plaintext tokens should be written directly into the JSON configuration file.
* B) Use environment variable expansion syntax (e.g., `${GITHUB_TOKEN}`) in `.mcp.json`.


* C) Encrypt the `.mcp.json` file using an unshared private key.
* D) Hardcode credentials inside system prompt text files.

6. A developer configures a personal experimental MCP server for testing local experimental tools that should NOT be shared with other team members via repository commits. Where should this configuration reside?

* A) In the project root `.mcp.json` file.
* B) In the user-scoped configuration file `~/.claude.json` in the developer's home directory.


* C) In the public `.claude/commands/` directory.
* D) In the shared repository `README.md` file.

7. An agent exploring an unfamiliar database structure repeatedly executes exploratory SQL tool queries, consuming token quota rapidly. How can MCP Resources be utilized to optimize this context overhead?

* A) Convert all SQL databases into static Markdown files manually before running Claude.
* B) Expose database schemas and catalogs as MCP Resources, providing passive schema visibility without incurring active exploratory tool-call roundtrips.


* C) Disable database access and force the agent to guess table column names.
* D) Increase the database query timeout limit to 60 minutes.

8. When exploring a large codebase to understand an end-to-end data processing workflow, what is the most context-efficient strategy for using built-in tools?

* A) Execute `Read` on every single file in the repository upfront before reasoning.
* B) Build codebase understanding incrementally: start with `Grep` or `Glob` to locate entry points, then use `Read` selectively to trace import paths.


* C) Use `Bash` to compile all source files into binary executables first.
* D) Run `Edit` on core source files to inject debug print statements everywhere.

9. Claude Code includes built-in tools like `Grep` alongside connected MCP tools like custom repository indexing tools. Developers notice Claude frequently chooses `Grep` over a far more powerful MCP code indexer. What fix addresses this tool selection bias?

* A) Delete `Grep` from the operating system host path.
* B) Enhance the MCP tool descriptions in configuration files to explicitly detail its superior search capability, indexing benefits, and output format over simple text searching.


* C) Disable all built-in tools in Claude Code.
* D) Force `tool_choice: "none"` on developer commands.

10. A developer wants to build a developer assistant skill that analyzes git commit history using the `Bash` tool. To ensure safety, the developer wants to prevent the skill from executing destructive file writing tools. How can this constraint be configured?

* A) Configure `allowed-tools` in the skill's frontmatter configuration to explicitly restrict available tools during skill execution.


* B) Delete the `Write` tool from the host filesystem prior to running the skill.
* C) Instruct the developer to monitor the terminal screen continuously and press Ctrl+C if a write occurs.
* D) Set the operating system filesystem to read-only permissions permanently.

11. What built-in tool in Claude Code allows direct execution of shell commands, and what primary precaution must developers implement when using it in automated workflows?

* A) `Bash`; sanitize inputs and restrict allowed command patterns to prevent unintended state modifications or security breaches.


* B) `Write`; ensure all file paths end with `.txt`.
* C) `Edit`; require full file overwrites on every modification.
* D) `Read`; limit file read chunks to 10 lines maximum.

12. When tracing exported functions across multiple wrapper modules in a complex codebase, how should an agent systematically track down dependencies?

* A) Read all files in alphabetical order starting from the root directory.
* B) Identify exported function names in entry modules, then use `Grep` to search for call occurrences across the codebase systematically.


* C) Rewrite the application in a different programming language.
* D) Delete wrapper modules one by one until runtime compilation fails.

13. What is the benefit of leveraging established community MCP servers (e.g., standard GitHub, Postgres, or Jira servers) compared to building custom MCP server implementations from scratch?

* A) Community servers bypass JSON schema validation entirely.
* B) They provide standardized, well-tested tool contracts, descriptions, and error handling out-of-the-box, saving effort and reducing integration bugs.


* C) Community servers do not require authentication tokens or credentials.
* D) They run without consuming API context tokens.

14. You have configured both a project-scoped `.mcp.json` file and a user-scoped `~/.claude.json` file on your system. At runtime in Claude Code, how are tools from these servers made available?

* A) User-scoped server tools completely override and disable project-scoped tools.
* B) Tools from all correctly configured MCP servers are discovered at connection time and made available simultaneously to the session.


* C) Only the first server declared in alphabetical order is loaded.
* D) Project-scoped tools are disabled unless the user runs as root administrator.

15. An agent needs to modify a specific method inside a 3,000-line source file. Under what condition should the `Edit` tool be preferred over the `Write` tool?

* A) When the modification target area has unique surrounding anchor text, allowing precise localized edits without rewriting the full 3,000 lines.


* B) When the source file contains no line breaks.
* C) When the target method appears 15 times identically throughout the file.
* D) When the agent has exceeded its API rate limit quota.

---

--- Answer Key & Explanations ---

Domain: 1 Agentic Architecture & Orchestration  with Scenario 1. Customer Support Resolution Agent

Question 1: C
Explanation: Programmatic prerequisite gating in code provides deterministic execution guarantees that prompt-based or few-shot approaches cannot achieve, ensuring mandatory security/financial compliance steps are never bypassed.

Question 2: B
Explanation: The control loop of an agentic system evaluates the `stop_reason` field returned by the API; `"tool_use"` indicates the loop must execute requested tools and return results, whereas `"end_turn"` signals task completion.

Question 3: B
Explanation: Decomposing multi-concern requests into parallel sub-investigations preserves context clarity and execution speed while producing a unified, coherent synthesis for the customer.

Question 4: A
Explanation: Tool call interception hooks run deterministically in code before target tool execution, allowing hard business thresholds (e.g., $500 refund limits) to be strictly enforced regardless of LLM generation variability.

Question 5: B
Explanation: Compiling a structured handoff summary (customer ID, verified root cause, refund status, action) provides human agents with instant actionable context without forcing them to parse verbose conversation logs.

Question 6: B
Explanation: Appending structured error outputs back into conversation context allows Claude to inspect explicit failure reasons and reason about safe, policy-compliant follow-up actions.

Question 7: A
Explanation: Transforming raw backend output via `Post ToolUse` hooks into standard clean formats (ISO timestamps, string statuses) optimizes token usage and eliminates model formatting confusion.

Question 8: B
Explanation: Fixed iteration caps can truncate valid complex multi-step workflows prematurely or waste resources; `stop_reason` inspection should drive loop execution state.

Question 9: B
Explanation: Model-driven tool selection dynamically adapts to unpredictable intermediate tool outputs and complex customer inputs, whereas rigid decision trees fail on unexpected edge cases.

Question 10: B
Explanation: Combining explicit prompt instructions with programmatic prerequisite gates in code ensures unambiguous operational boundaries and robust safety.

Question 11: A
Explanation: Resuming sessions with injected structured summaries prevents stale or historical tool result output from diluting current reasoning and bloating context windows.

Question 12: B
Explanation: System prompt instructions alone operate probabilistically and carry a non-zero failure rate; critical compliance rules require deterministic programmatic enforcement.

Question 13: B
Explanation: Marking transient errors with structured metadata (`isRetryable: true`) allows the agent orchestration loop to implement controlled retries without abandoning tasks.

Question 14: B
Explanation: In the Claude Agent SDK, conversation history containing assistant `tool_use` blocks and corresponding user `tool_result` blocks provides the context for subsequent reasoning turns.

Question 15: B
Explanation: Recognizing policy boundaries and triggering structured escalation protocols ensures exception cases receive authorized human review while preserving system auditability.

---

Domain: 1 Agentic Architecture & Orchestration  with Scenario 2. Multi-Agent Research System

Question 1: B
Explanation: Subagents instantiated in the Claude Agent SDK operate with isolated context windows and do not automatically inherit parent context; required context must be explicitly passed in prompts.

Question 2: A
Explanation: Spawning subagents requires invoking the `"Task"` tool, which must be explicitly declared in the coordinator agent's `allowedTools` list.

Question 3: B
Explanation: Overly narrow task decomposition by the coordinator leads to incomplete domain coverage across subagents, missing major facets of broad research topics.

Question 4: B
Explanation: Emitting multiple `Task` tool calls within a single response turn enables parallel subagent execution, significantly reducing multi-agent task execution latency.

Question 5: C
Explanation: Passing complete structured findings (including source metadata and claims) directly into the subagent's prompt payload guarantees that synthesis agents possess required context without relying on implicit state.

Question 6: C
Explanation: Returning structured error context (failure type, attempted actions, partial extractions) allows the coordinator agent to make intelligent recovery or fallback decisions.

Question 7: B
Explanation: Providing a scoped `verify_fact` tool directly to the synthesis agent handles lightweight verification needs efficiently without triggering heavy multi-turn coordinator roundtrips.

Question 8: B
Explanation: Hub-and-spoke multi-agent architectures require central communication routing through the coordinator to maintain system observability, consistent error handling, and structured state tracking.

Question 9: B
Explanation: `fork_session` allows parallel exploration branches to spawn from a shared analysis baseline without polluting the main session or requiring redundant exploration turns.

Question 10: B
Explanation: An iterative refinement loop allows the coordinator to evaluate synthesis completeness against coverage rubrics and issue targeted follow-up subagent tasks to fill identified research gaps.

Question 11: B
Explanation: Structured data formats (JSON/XML) cleanly separate narrative content from metadata (URLs, page numbers), preserving strict information provenance through multi-agent synthesis passes.

Question 12: A
Explanation: Goal-oriented prompts provide subagents with clear criteria while allowing them the operational flexibility to dynamically adapt tool calls based on findings.

Question 13: A
Explanation: Local error recovery within subagents handles transient infrastructure failures gracefully before escalating unresolvable issues to the coordinator.

Question 14: B
Explanation: Scoping tool access ensures agents only possess tools relevant to their functional role, preventing tool misuse or cross-domain execution errors.

Question 15: B
Explanation: Partitioning scope systematically by subtopics or source types ensures complete coverage across broad research tasks.

---

Domain: 2 Tool Design & MCP Integration  with Scenario 1. Customer Support Resolution Agent

Question 1: B
Explanation: Comprehensive tool descriptions outlining purpose, input formats, boundaries, and differentiation from similar tools are the primary mechanism LLMs use for accurate tool selection.

Question 2: C
Explanation: Returning `isError: true` accompanied by structured metadata (`errorCategory`, `isRetryable`, description) enables the agent to evaluate failures accurately and take appropriate recovery steps.

Question 3: B
Explanation: Restricting active tool access to a scoped, role-relevant set (4–5 tools) reduces decision complexity and significantly improves tool selection accuracy.

Question 4: C
Explanation: Forcing a specific tool via `tool_choice: {"type": "tool", "name": "..."}` guarantees that the named tool is executed on that turn.

Question 5: B
Explanation: Renaming tools and establishing explicit, non-overlapping input/output boundaries eliminates tool selection ambiguity for the model.

Question 6: A
Explanation: Structured error metadata provides explicit business context, allowing the agent to inform the user accurately without wasting turns on unretryable calls.

Question 7: A
Explanation: MCP Tools represent executable actions with side effects, whereas MCP Resources represent passive readable data catalogs or context sources.

Question 8: A
Explanation: Explicitly marking transient infrastructure failures as `isRetryable: true` prevents the agent runner from incorrectly assuming the operation failed due to invalid inputs or bad business logic.

Question 9: B
Explanation: `tool_choice: "any"` forces the model to execute at least one available tool turn rather than returning plain conversational text.

Question 10: B
Explanation: System prompt instructions containing specific keywords can inadvertently bias model decision-making toward matching tool names; prompts should be written to maintain clear boundaries.

Question 11: A
Explanation: Explicitly documenting edge cases and input boundary limits in parameter descriptions guides the model on proper argument formatting and handling ambiguous user queries.

Question 12: B
Explanation: Filtering backend database tool outputs down to essential key fields minimizes context token usage and prevents attention dilution during multi-turn reasoning.

Question 13: A
Explanation: Structured authorization error tags allow the agent control loop to trigger re-authentication or human escalation paths deterministically.

Question 14: A
Explanation: Discrete, single-purpose tools provide tight input schemas and clear functional boundaries, maximizing tool selection accuracy and maintainability.

Question 15: B
Explanation: Forced tool selection applies to the specified turn; on subsequent turns, `tool_choice` reverts to `"auto"`, allowing normal reasoning on tool output.

---

Domain: 2 Tool Design & MCP Integration  with Scenario 2. Developer Productivity with Claude

Question 1: B
Explanation: `Grep` is the built-in tool designated for searching file contents across codebases using regex patterns.

Question 2: B
Explanation: `Glob` matches file paths and naming patterns across directory trees using pattern expressions.

Question 3: B
Explanation: When localized `Edit` calls fail due to non-unique anchor text matches, reading the file full content (`Read`) followed by writing (`Write`) is the recommended fallback pattern.

Question 4: B
Explanation: Storing shared server definitions in project-scoped `.mcp.json` files committed to version control ensures all team members receive identical integrations.

Question 5: B
Explanation: Environment variable expansion (e.g., `${GITHUB_TOKEN}`) in `.mcp.json` prevents hardcoding secrets while permitting seamless runtime authentication.

Question 6: B
Explanation: Personal or experimental MCP configurations should be placed in user-scoped `~/.claude.json` files to prevent committing personal development configurations to repository tracking.

Question 7: B
Explanation: MCP Resources expose passive context sources (like schemas) without forcing the model to consume active tool call turns for exploratory lookups.

Question 8: B
Explanation: Incremental codebase discovery (starting with `Grep`/`Glob` patterns and selectively reading key entry points) preserves context tokens and maintains reasoning quality.

Question 9: B
Explanation: Enhancing MCP tool descriptions to clearly articulate superior indexing capabilities and output structure corrects model selection bias toward built-in tools.

Question 10: A
Explanation: Defining `allowed-tools` in skill frontmatter restricts active tool execution capabilities during skill execution, enforcing strict operational safety.

Question 11: A
Explanation: The `Bash` tool executes shell commands directly; proper input sanitization and command restriction are required to prevent arbitrary command execution.

Question 12: B
Explanation: Combining exported function identification with targeted `Grep` searches enables efficient systematic dependency tracing without reading entire directory trees.

Question 13: B
Explanation: Community MCP servers provide pre-tested, standardized interfaces for common services, reducing integration overhead and bug surface area.

Question 14: B
Explanation: Claude Code discovers and aggregates tools from both project-scoped `.mcp.json` and user-scoped `~/.claude.json` configurations simultaneously at startup.

Question 15: A
Explanation: The `Edit` tool is ideal for targeted modifications when unique anchor text exists, avoiding the unnecessary token cost and overwrite risks of full-file replaces.
