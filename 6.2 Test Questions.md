--- QUIZ ---

Domain: 1 Agentic Architecture & Orchestration with Scenario 1. Customer Support Resolution Agent

1. You are designing the agentic execution loop for a Customer Support Resolution Agent using the Claude Agent SDK. The agent needs to query customer details, inspect recent orders, and conditionally issue refunds. What is the correct control flow logic to govern the agentic loop execution and termination?

* A) Check if `stop_reason` equals `"tool_use"`; if so, execute the requested tools, append the tool results to the conversation history, and invoke the model again. Terminate the loop when `stop_reason` equals `"end_turn"`.
* B) Parse the text content of the assistant message for completion keywords such as "Resolved" or "Escalated", and set a hard cap of 5 maximum iterations as the primary loop termination condition.
* C) Execute the requested tools when `stop_reason` is `"tool_use"`, clear prior conversation history to save tokens, and pass only the latest tool response back to the model until `stop_reason` is `"max_tokens"`.
* D) Force `tool_choice: "any"` on every turn of the loop, terminating the agent loop when the model output contains no JSON tool call structures.

2. In your customer support system, high-value refunds over $500 must strictly require manager authorization before `process_refund` can be called. Relying purely on system prompt instructions resulted in occasional policy bypasses. Which SDK architecture pattern deterministically guarantees that unauthorized high-value refund calls are intercepted and blocked?

* A) Implement a `Post ToolUse` hook that parses the response from `process_refund` and rolls back the database transaction if the amount exceeds $500.
* B) Implement a tool call interception hook that inspects outgoing `process_refund` tool calls, validates the amount parameter against policy limits, blocks execution if over $500 without authorization, and redirects the workflow to human escalation.
* C) Configure `tool_choice: {"type": "tool", "name": "escalate_to_human"}` dynamically whenever the customer order history indicates an item price above $500.
* D) Pass the refund policy documentation inside an `@import` directive in the agent's system prompt and lower the model temperature to 0.0.

---

Domain: 1 Agentic Architecture & Orchestration with Scenario 3. Multi-Agent Research System

1. You are building a multi-agent research pipeline with a coordinator agent and four specialized subagents (web search, document analysis, synthesis, report generator). You notice that subagents are failing to reference findings discovered by previous subagents in the pipeline. What is the root cause of this behavior?

* A) Subagents automatically inherit the full conversation transcript from the coordinator, but their context windows are truncated due to token limit caps.
* B) Subagents operate with isolated contexts and do not automatically inherit the coordinator's conversation history; relevant findings from prior agents must be explicitly included in the subagent prompt or Task call payload.
* C) The coordinator failed to set `tool_choice: "auto"` when delegating tasks, preventing subagents from reading shared session storage.
* D) MCP resources were not registered at the user level (`~/.claude.json`), preventing subagents from accessing shared memory blocks across executions.

2. To maximize research throughput, the coordinator agent needs to trigger the web search subagent and document analysis subagent simultaneously when a user submits a broad research topic. How should the coordinator be configured and executed to achieve parallel subagent spawning?

* A) Configure sequential prompt chaining where the coordinator invokes the web search subagent, waits for `end_turn`, and then invokes the document analysis subagent on the next turn.
* B) Program the coordinator to emit multiple `Task` tool calls within a single model response turn, allowing the orchestrator to execute the subagents in parallel.
* C) Use `fork_session` to duplicate the coordinator process into two separate operating system threads before initiating subagent tool selection.
* D) Define a custom `Post ToolUse` hook that clones the coordinator's turn context and forwards it to two separate API endpoints concurrently.

---

Domain: 2 Tool Design & MCP Integration with Scenario 1. Customer Support Resolution Agent

1. Your customer support resolution agent uses custom Model Context Protocol (MCP) tools. When backend customer service microservices encounter transient database connection timeouts, the agent frequently informs the customer that their account does not exist. How should you redesign the tool error responses to resolve this issue?

* A) Standardize all tool failure outputs to return a plain text string `"Operation failed"` so the model treats all errors as generic system failures.
* B) Set the MCP `isError` flag to `false` and return an empty JSON object `{}` so the agent assumes no records were found.
* C) Return structured error metadata including `isError: true`, an `errorCategory` field (e.g., `transient`), an `isRetryable: true` boolean, and a descriptive message so the agent can execute retry logic or inform the user appropriately.
* D) Raise an unhandled HTTP 500 exception in the MCP server process to force the Claude Agent SDK loop to immediately terminate and restart the session.

2. The support resolution agent has access to 18 different tools, including specialized admin tools like `database_migration_execute` and `cache_flush`. During testing, the agent frequently selects incorrect tools or attempts administrative actions on standard customer queries. What design modification best adheres to Claude Architect tool distribution principles?

* A) Retain all 18 tools on the support agent but append explicit negative constraints in the system prompt for each irrelevant tool.
* B) Scope the support agent's tool set to 4–5 core tools directly relevant to its role (e.g., `get_customer`, `lookup_order`, `process_refund`, `escalate_to_human`), removing admin tools from its available context.
* C) Change `tool_choice` to `"any"` on every turn to force the model to select tools purely at random until a valid result is returned.
* D) Combine all 18 tools into a single monolithic MCP tool named `execute_support_action` with a generic JSON schema input.

---

Domain: 2 Tool Design & MCP Integration with Scenario 4. Developer Productivity with Claude

1. An enterprise software team is configuring custom Model Context Protocol (MCP) tools to allow Claude Code to inspect internal Jira tickets and query GitHub pull requests. The development team needs to share common server configurations across the team repository without hardcoding sensitive personal access tokens into source control. How should this be configured?

* A) Define project-level MCP tools inside `~/.claude.json` and hardcode the developer tokens directly into the executable path arguments.
* B) Place configuration in a project-scoped `.mcp.json` file at the root of the repository, using environment variable expansion syntax (e.g., `${GITHUB_TOKEN}`) for secret credentials.
* C) Embed the GitHub access token inside a project-level `CLAUDE.md` file using the `@import` directive.
* D) Configure user-scoped `~/.claude/CLAUDE.md` files on each machine with plaintext API key credentials embedded in the system prompt.

2. A developer productivity agent needs to locate all invocations of a deprecated function `calculateTax()` across a massive 10,000-file TypeScript repository. Which built-in tool strategy provides the most efficient and reliable initial code discovery?

* A) Execute `Read` on all 10,000 files sequentially from the root directory to inspect their text content directly.
* B) Use `Glob` with the pattern `**/*.ts` to load full file contents into context for string matching.
* C) Use `Grep` with a regular expression pattern matching `calculateTax` across the codebase to pinpoint exact file paths and line numbers without reading whole files into context upfront.
* D) Run `Edit` on the root index file to trigger a full AST re-parse across all package dependencies.

---

Domain: 3 Claude Code Configuration & Workflows with Scenario 2. Code Generation with Claude Code

1. A team lead wants to establish project-wide code style rules and unit testing commands for Claude Code that apply to all developers on the team when working in the repository. A newly onboarded developer reports that Claude Code is ignoring these rules on their machine. Investigation reveals the team lead placed the rules in `~/.claude/CLAUDE.md`. What is the architectural solution to fix this issue?

* A) Instruct the new developer to manually copy the lead's `~/.claude/CLAUDE.md` into their own home directory `~/.claude/` location.
* B) Move the configuration to a project-scoped `.claude/CLAUDE.md` (or root `CLAUDE.md`) file within the repository and commit it to version control so all team members inherit it automatically.
* C) Add a `Post ToolUse` hook in `.mcp.json` that injects user home directory paths into the agent context window at runtime.
* D) Instruct the team lead to set `tool_choice: "forced"` inside `~/.claude.json` for all repository contributors.

2. A complex monorepo contains multiple packages (`/services/auth`, `/services/payments`, `/apps/web`). The project root `CLAUDE.md` has become a monolithic 3,000-line file that consumes excessive context tokens and causes instruction confusion. How should the team refactor their Claude Code configuration?

* A) Delete the root `CLAUDE.md` and rely exclusively on user-level `~/.claude.json` settings on each developer's laptop.
* B) Split the monolithic file into topic-specific rule files within `.claude/rules/` (e.g., `testing.md`, `api-conventions.md`) and use directory-level `CLAUDE.md` files with `@import` syntax where appropriate.
* C) Convert all markdown configuration files into MCP tool definitions and register them as executable HTTP endpoints.
* D) Embed all rules into a single custom slash command that developers must manually trigger before every code generation prompt.

---

Domain: 3 Claude Code Configuration & Workflows with Scenario 5. Claude Code for Continuous Integration

1. You are configuring Claude Code in a GitHub Actions CI pipeline to perform automated pull request reviews. To prevent excessive false positives on PRs, how should the review prompt and execution workflow be structured?

* A) Execute a single unconstrained prompt asking Claude Code to "find every potential bug and style issue in the repository."
* B) Split the review into a two-pass architecture using prompt chaining: first run local file analysis passes on modified files, followed by a dedicated cross-file integration pass focusing on public API contracts and breaking changes.
* C) Force `tool_choice: "any"` on the `Edit` tool so Claude automatically rewrites all modified files without returning analysis text.
* D) Instruct Claude Code via `CLAUDE.md` to flag every line of modified code that does not contain inline JSDoc comments.

2. During a long multi-turn refactoring session with Claude Code, several source files were manually edited and recompiled outside of the Claude Code terminal session. When resuming the work using `--resume refactor-session`, Claude Code generates edits based on outdated file contents. What is the recommended practice to ensure state consistency?

* A) Never use `--resume`; always wipe the repository history and start every work session without context.
* B) Run `fork_session` to create a duplicate session and assume context auto-synchronizes with git disk status.
* C) Explicitly inform the resumed session about the specific modified files so Claude Code performs targeted re-reads, or start a new session with an injected structured summary if prior tool results are stale.
* D) Delete the project-level `.mcp.json` file and restart the local daemon process.

---

Domain: 4 Prompt Engineering & Structured Output with Scenario 5. Claude Code for Continuous Integration

1. Your automated CI code review tool needs to output JSON-formatted security audit reports that strictly adhere to a schema for consumption by downstream ticket creation microservices. What design approach yields the most reliable structured JSON output from Claude?

* A) Provide natural language instructions in the prompt asking Claude to "output valid JSON only", without providing a schema or few-shot examples.
* B) Define an explicit JSON Schema, pass few-shot input/output examples within the prompt, and leverage JSON mode or structured outputs / tool calling constructs to enforce schema validation.
* C) Set model temperature to 1.0 and parse the assistant's natural language narrative output using regex in a post-processing script.
* D) Wrap the request in a loop that retries identical prompts up to 20 times whenever JSON parsing fails in CI.

2. In your automated test generation pipeline, Claude Code generates unit tests for modified source files. To ensure generated tests cover edge cases without inventing non-existent library helper functions, how should the generation prompt be engineered?

* A) Instruct Claude to write minimal generic test stubs without importing any external assertions or helper modules.
* B) Provide few-shot examples of existing, valid test suites from the project, explicitly include signatures of available test utility functions, and specify exact failure scenarios to test.
* C) Set `tool_choice: "none"` so Claude cannot read existing test patterns from disk.
* D) Omit source file context from the prompt and allow Claude to infer class structures strictly from file names.

---

Domain: 5 Context Management & Reliability with Scenario 6. Structured Data Extraction

1. You are building an automated document extraction pipeline that extracts financial metadata from 100-page unstructured PDF transcripts. Processing the entire transcript in a single prompt context causes attention dilution, leading to missing key line items. Which context management architecture solves this problem?

* A) Pass the entire 100-page transcript in a single user message and append "Pay ultra close attention to all numbers" at the end.
* B) Decompose the document processing pipeline using prompt chaining or subagents: partition the document into logical sections, perform extraction passes per section, and aggregate extracted structured outputs into a final summary pass.
* C) Increase the model temperature to 0.9 to encourage creative retrieval of missing table values.
* D) Disable JSON schema validation and rely on conversational free-form output to save context tokens.

2. A structured data extraction agent encounters ambiguous invoice documents where required tax identification numbers are missing or illegible. Rather than hallucinating values or failing silently, how should the agent be architected for reliable operation?

* A) Return a mock tax ID number derived from the current timestamp so downstream pipelines do not fail validation.
* B) Design the tool/output contract to return structured status codes indicating validation failure, include specific error reasoning, and trigger a human-in-the-loop escalation workflow with a summarized handoff package.
* C) Loop infinitely using the `Bash` tool until the file on disk updates with legible content.
* D) Set `stop_reason` to `"max_tokens"` forcibly whenever an ambiguous document field is detected.

---

--- Answer Key & Explanations ---

Domain: 1 Agentic Architecture & Orchestration with Scenario 1. Customer Support Resolution Agent

Question 1: A

Explanation: Task Statement 1.1 specifies that standard agentic loop control flow must inspect `stop_reason`. When `stop_reason` is `"tool_use"`, the application executes the requested tool(s), appends the results to the conversation history, and invokes Claude again. The loop properly terminates when `stop_reason` is `"end_turn"`. Natural language parsing or arbitrary iteration caps are explicitly identified as anti-patterns.

Question 2: B

Explanation: Task Statement 1.5 emphasizes that prompt instructions alone provide probabilistic compliance, whereas SDK hooks provide deterministic enforcement. A pre-tool interception hook inspects outgoing tool parameters (such as refund amounts over $500) and blocks execution or redirects to escalation before the action is executed.

---

Domain: 1 Agentic Architecture & Orchestration with Scenario 3. Multi-Agent Research System

Question 1: B

Explanation: Task Statements 1.2 and 1.3 specify that subagents operate with isolated contexts and do not automatically inherit parent or coordinator conversation history. Relevant outputs from prior subagents must be explicitly passed within the subagent's prompt or Task invocation context.

Question 2: B

Explanation: Task Statement 1.3 specifies that parallel subagents are spawned by emitting multiple `Task` tool calls within a single coordinator model response turn rather than across separate sequential turns.

---

Domain: 2 Tool Design & MCP Integration with Scenario 1. Customer Support Resolution Agent

Question 1: C

Explanation: Task Statement 2.2 states that uniform or generic error messages prevent the model from making informed recovery decisions. Returning structured error metadata (including `isError: true`, `errorCategory`, `isRetryable`, and human-readable context) enables Claude to distinguish transient retriable errors from permanent failures.

Question 2: B

Explanation: Task Statement 2.3 highlights that giving an agent too many tools (e.g., 18 instead of 4–5) increases decision complexity and degrades tool selection reliability. Scoping each agent's available tools strictly to its role eliminates misuse of out-of-scope administrative functions.

---

Domain: 2 Tool Design & MCP Integration with Scenario 4. Developer Productivity with Claude

Question 1: B

Explanation: Task Statement 2.4 specifies that project-level MCP tools should be configured in `.mcp.json` at the project root using environment variable expansion (such as `${GITHUB_TOKEN}`) to manage credentials without committing secrets to version control. `~/.claude.json` is reserved for user-scoped configurations.

Question 2: C

Explanation: Task Statement 2.5 defines `Grep` as the ideal built-in tool for content pattern searching across file contents. Using `Grep` pinpoints function calls efficiently without loading whole file contents into the context window upfront like `Read` or `Glob`.

---

Domain: 3 Claude Code Configuration & Workflows with Scenario 2. Code Generation with Claude Code

Question 1: B

Explanation: Task Statement 3.1 explains that configuration files located in user home directories (`~/.claude/CLAUDE.md`) apply only to that specific user and are not shared with teammates. Project-scoped configurations (`.claude/CLAUDE.md` or root `CLAUDE.md`) committed to repository source control ensure uniform rule inheritance across the team.

Question 2: B

Explanation: Task Statement 3.1 recommends organizing topic-specific rule files under `.claude/rules/` or using directory-level `CLAUDE.md` files with `@import` syntax to keep configurations modular and prevent monolithic file maintenance issues.

---

Domain: 3 Claude Code Configuration & Workflows with Scenario 5. Claude Code for Continuous Integration

Question 1: B

Explanation: Task Statement 1.6 recommends decomposing complex multi-file reviews into separate local file analysis passes followed by a cross-file integration pass to prevent attention dilution and minimize false positive reports in automated CI workflows.

Question 2: C

Explanation: Task Statement 1.7 highlights that when resuming sessions after external code modifications, developers should explicitly inform the agent about modified files so targeted re-analysis occurs, or start fresh with an injected summary if prior tool outputs have become stale.

---

Domain: 4 Prompt Engineering & Structured Output with Scenario 5. Claude Code for Continuous Integration

Question 1: B

Explanation: Section 6 Domain 4 objectives outline that reliable structured JSON generation requires explicit JSON schemas, targeted few-shot examples, and strict output formatting constraints (such as tool calling or structured output features) rather than unconstrained natural language instructions.

Question 2: B

Explanation: Effective prompt engineering for code and test generation relies on providing concrete few-shot examples, explicitly documenting available library interface signatures, and detailing exact boundary cases to avoid hallucinated dependencies.

---

Domain: 5 Context Management & Reliability with Scenario 6. Structured Data Extraction

Question 1: B

Explanation: Section 6 Domain 5 guidelines highlight that long documents cause attention dilution when passed entirely in one turn. Partitioning the document into sections, running section-level extraction pipelines, and aggregating the results avoids token limit degradation and missing information.

Question 2: B

Explanation: Task Statement 1.4 and Section 6 Domain 5 reliability guidelines specify that when data is ambiguous or invalid, systems should produce structured error flags/status codes and trigger structured handoff summaries for human-in-the-loop escalation rather than hallucinating output.
